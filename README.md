# Quiz App (Assignment 4)\n\nFull-stack quiz application built with Express/MongoDB backend and React + Redux frontend.\n\n## Prerequisites\n- Node.js 18+\n- MongoDB running locally (default `mongodb://127.0.0.1:27017/quizapp`)\n\n## Backend (be)\n```bash\ncd be\nnpm install\nnpm run seed   # optional: seeds admin/user, quiz, questions, article\nnpm run dev    # starts at http://localhost:4000\n```\n\nEnv vars: create `.env` in `be/`\n```\nPORT=4000\nMONGO_URI=mongodb://127.0.0.1:27017/quizapp\nJWT_SECRET=supersecret\n```\n\nDefault seeded accounts:\n- admin / admin123 (role: admin)\n- john / john123 (role: user)\n\n## Frontend (fe)\n```bash\ncd fe\nnpm install\nnpm run dev   # Vite dev server at http://localhost:3001\n```\n\nFrontend expects backend at `http://localhost:4000`. Adjust `fe/src/utils/api.js` if needed.\n\n## Features\n- Auth: register/login with JWT, role-based routes (user vs admin)\n- Users: list quizzes, take quiz, see score, view articles\n- Admin: CRUD quizzes, questions, articles via dashboard\n- Styling: Bootstrap 5\n\n## Manual Test Matrix\n- Auth: register, login (user/admin), logout, guard redirects\n- User: fetch quizzes, start quiz, select answers, submit score, restart, view articles\n- Admin: create/update/delete quiz, add/delete question, create/delete article\n- API errors: invalid login, missing token access, deleting quiz removes questions\n\n## Notes\n- Data validations on backend via express-validator\n- Tokens stored in localStorage; axios interceptor injects Authorization header\n*** End Patch"## Heading ##

